import React from 'react'
import {Container} from './styles'
import PropTypes from 'prop-types'

function <%= props.name %>Component() {
	return (
		<Container>
			
		</Container>
	)
}

<%=props.name%>Component.propTypes = {
	
}

<%=props.name%>Component.defaultProps = {

}

export default function <%= props.name %>Component